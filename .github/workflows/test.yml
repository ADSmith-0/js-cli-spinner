on:
  workflow_dispatch

jobs:
  run-node-script:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v5

    - name: Install node
      uses: actions/setup-node@v4
      with:
        node-version: "24.x"

    - name: Print variable
      run: node index.ts
